# ASYMPTOTE 品牌图形

- 正式标题艺术 Logo 建议命名为 `asymptote-title-artwork.svg`。
- 也支持 PNG 或 WebP；文件名可以自定。
- 放入此目录后，在 `src/config/brand.ts` 的 `titleArtwork` 中填写 `/images/brand/文件名`。
- 当前未配置文件时，首页继续使用 `TitleArtwork.astro` 内置的极淡 SVG 占位图。

请只放置原创或拥有明确使用授权的素材。
