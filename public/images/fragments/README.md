# ASYMPTOTE 五分区图片

- WRITING、IMAGE、CINEMA、MUSIC、GAMES 的正式图片放在此目录。
- 放入后，在 `src/config/fragments.ts` 对应项目的 `image` 字段填写 `/images/fragments/文件名`。
- 如果主体需要避开文字或斜切边界，可同时调整对应项目的 `imagePosition`。
- 页面使用 `background-size: cover`，图片会按倾斜切面裁切，不保证展示完整原图。

请只放置原创或拥有明确使用授权的素材。
